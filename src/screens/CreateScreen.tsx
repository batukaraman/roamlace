import React, {Component} from 'react';
import {Text, StyleSheet, View} from 'react-native';

export default class CreateScreen extends Component {
  render() {
    return (
      <View>
        <Text> Seyahat Oluşturma Ekranı </Text>
      </View>
    );
  }
}

const styles = StyleSheet.create({});
